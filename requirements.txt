# requirements.txt
# Install all dependencies with:
#   pip install -r requirements.txt

# Core data engineering
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0       # Parquet file format (fast columnar storage)

# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9   # PostgreSQL adapter
python-dotenv==1.0.1     # Load .env files

# Dashboard / visualisation
streamlit==1.35.0
plotly==5.22.0

# Optional: for connecting to real online data sources
requests==2.32.2
openpyxl==3.1.3         # Excel files
